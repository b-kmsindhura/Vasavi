import { useEffect, useMemo, useState } from 'react';
import { ArrowRight, ChevronDown, ChevronLeft, Menu, Plus, Search, SlidersHorizontal, Sparkles, Trash2, Upload, X } from 'lucide-react';

const seedDesigns = [
  { id: 1, name: 'Rose Gold Zari', price: 1799, category: 'Bridal', fabric: 'Silk', workType: 'Zari embroidery', color: 'Rose', sizes: ['S', 'M', 'L', 'XL'], description: 'A softly structured bridal blouse with hand-finished zari vines and a warm rose tone.', image: 'https://images.unsplash.com/photo-1610030469983-98e550d6193c?auto=format&fit=crop&w=900&q=85', createdAt: '2025-09-01' },
  { id: 2, name: 'Saffron Silk', price: 699, category: 'Simple', fabric: 'Silk', workType: 'Minimal', color: 'Saffron', sizes: ['S', 'M', 'L'], description: 'Clean lines and a luminous silk finish for everyday celebrations.', image: 'https://images.unsplash.com/photo-1594633312681-425c7b97ccd1?auto=format&fit=crop&w=900&q=85', createdAt: '2025-10-12' },
  { id: 3, name: 'Mogra Maggam', price: 2199, category: 'Traditional', fabric: 'Raw silk', workType: 'Maggam work', color: 'Ivory', sizes: ['M', 'L', 'XL'], description: 'A heirloom-inspired silhouette with dimensional floral maggam work.', image: 'https://images.unsplash.com/photo-1583391733956-6c78276477e2?auto=format&fit=crop&w=900&q=85', createdAt: '2025-11-02' },
  { id: 4, name: 'Night Garden', price: 1499, category: 'Designer', fabric: 'Velvet', workType: 'Thread work', color: 'Black', sizes: ['S', 'M', 'L', 'XL'], description: 'Midnight velvet detailed with a quiet constellation of floral threadwork.', image: 'https://images.unsplash.com/photo-1566174053879-31528523f8ae?auto=format&fit=crop&w=900&q=85', createdAt: '2025-12-09' },
  { id: 5, name: 'Marigold Sequin', price: 999, category: 'Party Wear', fabric: 'Georgette', workType: 'Sequin', color: 'Gold', sizes: ['S', 'M', 'L'], description: 'Light-catching sequins arranged to feel festive, never fussy.', image: 'https://images.unsplash.com/photo-1539109136881-3be0616acf4b?auto=format&fit=crop&w=900&q=85', createdAt: '2026-01-11' },
  { id: 6, name: 'Madder Bloom', price: 1299, category: 'Wedding', fabric: 'Raw silk', workType: 'Floral embroidery', color: 'Red', sizes: ['S', 'M', 'L', 'XL'], description: 'A rich madder-red base with an expressive, hand-drawn floral motif.', image: 'https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=900&q=85', createdAt: '2026-01-25' },
  { id: 7, name: 'Pearl Petal', price: 1899, category: 'Bridal', fabric: 'Satin', workType: 'Pearl work', color: 'Blush', sizes: ['S', 'M', 'L'], description: 'Pearl edging and a sculpted neckline for the modern bride.', image: 'https://images.unsplash.com/photo-1551488831-00ddcb6c6bd3?auto=format&fit=crop&w=900&q=85', createdAt: '2026-02-04' },
  { id: 8, name: 'Indigo Lines', price: 549, category: 'Traditional', fabric: 'Cotton silk', workType: 'Block print', color: 'Indigo', sizes: ['S', 'M', 'L', 'XL'], description: 'A relaxed cotton-silk blouse finished with a graphic indigo border.', image: 'https://images.unsplash.com/photo-1496747611176-843222e1e57c?auto=format&fit=crop&w=900&q=85', createdAt: '2026-02-14' },
  { id: 9, name: 'Champagne Leaf', price: 1599, category: 'Wedding', fabric: 'Silk', workType: 'Aari work', color: 'Champagne', sizes: ['M', 'L', 'XL'], description: 'Fine aari leaves drift across a champagne silk base.', image: 'https://images.unsplash.com/photo-1509631179647-0177331693ae?auto=format&fit=crop&w=900&q=85', createdAt: '2026-03-01' },
  { id: 10, name: 'Cedar Check', price: 799, category: 'Simple', fabric: 'Cotton', workType: 'Woven', color: 'Brown', sizes: ['S', 'M', 'L'], description: 'A tactile woven check for sarees that already make an entrance.', image: 'https://images.unsplash.com/photo-1571945153237-4929e783af4a?auto=format&fit=crop&w=900&q=85', createdAt: '2026-03-08' },
  { id: 11, name: 'Lotus Cutwork', price: 2499, category: 'Designer', fabric: 'Brocade', workType: 'Cutwork', color: 'Pink', sizes: ['S', 'M', 'L', 'XL'], description: 'A statement brocade with considered lotus cutwork at the back.', image: 'https://images.unsplash.com/photo-1591369822096-ffd140ec948f?auto=format&fit=crop&w=900&q=85', createdAt: '2026-03-16' },
  { id: 12, name: 'Cobalt Draped', price: 1199, category: 'Party Wear', fabric: 'Satin', workType: 'Draped detail', color: 'Blue', sizes: ['S', 'M', 'L'], description: 'A fluid cobalt blouse with an elegant draped shoulder detail.', image: 'https://images.unsplash.com/photo-1566206091558-7f218b696731?auto=format&fit=crop&w=900&q=85', createdAt: '2026-03-21' },
];
const ranges = [{ label: 'Under ₹500', min: 0, max: 499 }, { label: '₹500 — ₹1,000', min: 500, max: 1000 }, { label: '₹1,000 — ₹1,500', min: 1001, max: 1500 }, { label: '₹1,500 — ₹2,000', min: 1501, max: 2000 }, { label: 'Above ₹2,000', min: 2001, max: Infinity }];
const categories = ['All categories', 'Bridal', 'Wedding', 'Party Wear', 'Traditional', 'Simple', 'Designer'];
const money = (value) => `₹${value.toLocaleString('en-IN')}`;

function App() {
  const [page, setPage] = useState(window.location.hash.replace('#', '') || 'home');
  const [designs, setDesigns] = useState(() => JSON.parse(localStorage.getItem('vasavi-designs') || 'null') || seedDesigns);
  const [selected, setSelected] = useState(null);
  const [menuOpen, setMenuOpen] = useState(false);
  const [activeRange, setActiveRange] = useState(null);
  useEffect(() => { localStorage.setItem('vasavi-designs', JSON.stringify(designs)); }, [designs]);
  const go = (next) => { setPage(next); setMenuOpen(false); window.location.hash = next; window.scrollTo(0, 0); };
  const openDesign = (design) => { setSelected(design); go('details'); };
  return <div className="app"><Header page={page} go={go} menuOpen={menuOpen} setMenuOpen={setMenuOpen} />
    {page === 'home' && <Home go={go} setActiveRange={setActiveRange} />}
    {page === 'designs' && <Designs designs={designs} openDesign={openDesign} initialRange={activeRange} />}
    {page === 'details' && <Details design={selected || designs[0]} go={go} />}
    {page === 'about' && <About go={go} />}
    {page === 'admin' && <Admin designs={designs} setDesigns={setDesigns} go={go} />}
    <Footer go={go} />
  </div>;
}

function Header({ page, go, menuOpen, setMenuOpen }) { return <header className="site-header"><div className="header-inner"><button className="wordmark" onClick={() => go('home')}>Vasavi<span>Boutiques</span></button><button className="menu-toggle" aria-label="Toggle menu" onClick={() => setMenuOpen(!menuOpen)}>{menuOpen ? <X /> : <Menu />}</button><nav className={menuOpen ? 'nav open' : 'nav'}>{[['home', 'Home'], ['designs', 'Designs'], ['about', 'About'], ['admin', 'Admin']].map(([key, label]) => <button className={page === key ? 'nav-link active' : 'nav-link'} onClick={() => go(key)} key={key}>{label}</button>)}<button className="nav-cta" onClick={() => go('designs')}>Explore collection <ArrowRight size={15} /></button></nav></div></header>; }
function Home({ go, setActiveRange }) { return <main><section className="hero"><div className="hero-copy"><p className="eyebrow"><Sparkles size={14} /> The blouse edit / 01</p><h1>Made to be<br /><em>remembered.</em></h1><p className="hero-sub">A considered collection of blouse designs for the sarees, celebrations, and stories that stay with you.</p><div className="hero-actions"><button className="button primary" onClick={() => go('designs')}>View designs <ArrowRight size={16} /></button><button className="button text-button" onClick={() => { setActiveRange(2); go('designs'); }}>Browse by price <ChevronDown size={16} /></button></div><div className="hero-note"><span>12</span> designs, thoughtfully made for every occasion</div></div><div className="hero-image"><img src="https://images.unsplash.com/photo-1595777457583-95e059d581b8?auto=format&fit=crop&w=1200&q=90" alt="Woman in an elegant rose blouse" /><div className="image-caption">Vasavi / SS26 <span>01 — 04</span></div></div></section><section className="manifesto"><p className="eyebrow">A softer kind of statement</p><h2>Details that speak<br /><em>without saying too much.</em></h2><p>From quiet everyday silhouettes to the one you will wear down the aisle, each piece is chosen for its balance of craft, comfort, and character.</p></section><PriceRanges go={go} setActiveRange={setActiveRange} /><section className="home-footer-cta"><p className="eyebrow">Find your signature</p><h2>Something beautiful<br /><em>is waiting.</em></h2><button className="button primary" onClick={() => go('designs')}>See all designs <ArrowRight size={16} /></button></section></main>; }
function PriceRanges({ go, setActiveRange }) { return <section className="price-section"><div className="section-heading"><div><p className="eyebrow">Shop with intention</p><h2>Explore by <em>price.</em></h2></div><p>Start where it feels right.<br />Find the details from there.</p></div><div className="range-grid">{ranges.map((range, index) => <button key={range.label} className="range-card" onClick={() => { setActiveRange(index); go('designs'); }}><span>0{index + 1}</span><strong>{range.label}</strong><ArrowRight size={16} /></button>)}</div></section>; }
function Designs({ designs, openDesign, initialRange }) { const [search, setSearch] = useState(''); const [category, setCategory] = useState('All categories'); const [range, setRange] = useState(initialRange ?? null); const [sort, setSort] = useState('Newest'); const filtered = useMemo(() => { let result = designs.filter((d) => { const haystack = `${d.name} ${d.category} ${d.color} ${d.fabric} ${d.workType}`.toLowerCase(); const inSearch = haystack.includes(search.toLowerCase()); const inCategory = category === 'All categories' || d.category === category; const selectedRange = range === null ? true : d.price >= ranges[range].min && d.price <= ranges[range].max; return inSearch && inCategory && selectedRange; }); return result.sort((a, b) => sort === 'Price: Low to High' ? a.price - b.price : sort === 'Price: High to Low' ? b.price - a.price : new Date(b.createdAt) - new Date(a.createdAt)); }, [designs, search, category, range, sort]); return <main className="catalogue"><div className="catalogue-intro"><div><p className="eyebrow">The complete edit / {String(designs.length).padStart(2, '0')} pieces</p><h1>Find your <em>perfect</em><br />blouse.</h1></div><p>Browse by mood, material, or simply<br />by the feeling you want to carry.</p></div><div className="filter-bar"><label className="search-field"><Search size={17} /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search designs, colours, fabrics..." /></label><label className="select-field"><span>Category</span><select value={category} onChange={(e) => setCategory(e.target.value)}>{categories.map((item) => <option key={item}>{item}</option>)}</select><ChevronDown size={15} /></label><label className="select-field"><span>Sort by</span><select value={sort} onChange={(e) => setSort(e.target.value)}><option>Newest</option><option>Price: Low to High</option><option>Price: High to Low</option></select><ChevronDown size={15} /></label></div><div className="range-pills"><button className={range === null ? 'pill selected' : 'pill'} onClick={() => setRange(null)}><SlidersHorizontal size={14} /> All designs</button>{ranges.map((item, i) => <button className={range === i ? 'pill selected' : 'pill'} onClick={() => setRange(i)} key={item.label}>{item.label}</button>)}</div>{filtered.length ? <div className="design-grid">{filtered.map((design, i) => <DesignCard design={design} openDesign={openDesign} key={design.id} index={i} />)}</div> : <div className="empty-state"><Sparkles size={22} /><h2>No designs found</h2><p>Try another price range, category, or search term.</p><button className="button secondary" onClick={() => { setSearch(''); setCategory('All categories'); setRange(null); }}>Clear filters</button></div>}</main>; }
function DesignCard({ design, openDesign, index }) { return <article className="design-card" style={{ '--delay': `${index * 45}ms` }}><button className="card-image" onClick={() => openDesign(design)}><img src={design.image} alt={design.name} /><span className="card-number">{String(index + 1).padStart(2, '0')}</span></button><div className="card-info"><div><p className="card-category">{design.category} / {design.color}</p><h3>{design.name}</h3></div><strong>{money(design.price)}</strong></div><button className="card-link" onClick={() => openDesign(design)}>View details <ArrowRight size={14} /></button></article>; }
function Details({ design, go }) { if (!design) return null; return <main className="details"><button className="back-link" onClick={() => go('designs')}><ChevronLeft size={16} /> Back to designs</button><div className="detail-layout"><div className="detail-image"><img src={design.image} alt={design.name} /></div><div className="detail-copy"><p className="eyebrow">{design.category} collection</p><h1>{design.name}</h1><p className="detail-price">{money(design.price)}</p><p className="detail-description">{design.description}</p><div className="spec-list"><Spec label="Fabric" value={design.fabric} /><Spec label="Work" value={design.workType} /><Spec label="Colour" value={design.color} /></div><div className="size-row"><span>Available sizes</span><div>{design.sizes.map((size) => <b key={size}>{size}</b>)}</div></div><a className="button primary whatsapp" href={`https://wa.me/?text=${encodeURIComponent(`Hi, I am interested in the ${design.name} priced at ${money(design.price)}.`)}`} target="_blank" rel="noreferrer">Enquire on WhatsApp <ArrowRight size={16} /></a><p className="detail-footnote">Made to order · Please enquire for availability and customisation.</p></div></div></main>; }
function Spec({ label, value }) { return <div><span>{label}</span><strong>{value}</strong></div>; }
function About({ go }) { return <main className="about"><div className="about-heading"><p className="eyebrow">Our point of view</p><h1>Less, but <em>better.</em></h1></div><div className="about-content"><div className="about-image"><img src="https://images.unsplash.com/photo-1550614000-4b9519e0927e?auto=format&fit=crop&w=1000&q=85" alt="Textile detail in warm natural light" /></div><div className="about-text"><p>Vasavi Boutiques began with a simple belief: the blouse should feel like a small piece of art, not an afterthought.</p><p>We look for the quiet details. A neckline that changes the way a saree falls. A colour that catches the evening light. Craft that feels considered, and comfort that lets you forget you are wearing it.</p><p>Our collection is edited in small batches, with a focus on beautiful materials, thoughtful workmanship, and pieces that earn their place in your wardrobe.</p><button className="button primary" onClick={() => go('designs')}>Explore the edit <ArrowRight size={16} /></button></div></div></main>; }
function Admin({ designs, setDesigns, go }) { const [authed, setAuthed] = useState(false); const [view, setView] = useState('manage'); const [form, setForm] = useState({ name: '', price: '', category: 'Bridal', description: '', fabric: '', workType: '', color: '', sizes: 'S, M, L, XL', image: '' }); const update = (key, value) => setForm({ ...form, [key]: value }); const submit = (e) => { e.preventDefault(); if (!form.name || !form.price || !form.image) return; setDesigns([{ ...form, id: Date.now(), price: Number(form.price), sizes: form.sizes.split(',').map((x) => x.trim()), createdAt: new Date().toISOString() }, ...designs]); setForm({ name: '', price: '', category: 'Bridal', description: '', fabric: '', workType: '', color: '', sizes: 'S, M, L, XL', image: '' }); setView('manage'); }; const remove = (id) => { if (window.confirm('Are you sure you want to delete this design?')) setDesigns(designs.filter((d) => d.id !== id)); }; if (!authed) return <main className="admin-login"><div className="login-panel"><p className="eyebrow">Private studio</p><h1>Welcome <em>back.</em></h1><p>Sign in to curate your collection.</p><form onSubmit={(e) => { e.preventDefault(); setAuthed(true); }}><label>Email<input type="email" required placeholder="you@example.com" /></label><label>Password<input type="password" required placeholder="••••••••" /></label><button className="button primary" type="submit">Enter studio <ArrowRight size={16} /></button></form><button className="back-link" onClick={() => go('home')}><ChevronLeft size={16} /> Return to site</button></div></main>; return <main className="admin"><div className="admin-head"><div><p className="eyebrow">Private studio / {designs.length} designs</p><h1>Curate your <em>collection.</em></h1></div><button className="button secondary" onClick={() => go('home')}>View live site <ArrowRight size={15} /></button></div><div className="admin-tabs"><button className={view === 'manage' ? 'tab active' : 'tab'} onClick={() => setView('manage')}>Manage designs</button><button className={view === 'add' ? 'tab active' : 'tab'} onClick={() => setView('add')}><Plus size={15} /> Add new design</button></div>{view === 'manage' ? <div className="manage-list">{designs.map((d) => <div className="manage-row" key={d.id}><img src={d.image} alt="" /><div><strong>{d.name}</strong><span>{d.category} · {money(d.price)}</span></div><button className="icon-button" aria-label={`Delete ${d.name}`} onClick={() => remove(d.id)}><Trash2 size={17} /></button></div>)}</div> : <form className="design-form" onSubmit={submit}><div className="form-grid"><label>Design name<input value={form.name} onChange={(e) => update('name', e.target.value)} required /></label><label>Price (₹)<input type="number" value={form.price} onChange={(e) => update('price', e.target.value)} required /></label><label>Category<select value={form.category} onChange={(e) => update('category', e.target.value)}>{categories.slice(1).map((c) => <option key={c}>{c}</option>)}</select></label><label>Colour<input value={form.color} onChange={(e) => update('color', e.target.value)} /></label><label>Fabric<input value={form.fabric} onChange={(e) => update('fabric', e.target.value)} /></label><label>Work type<input value={form.workType} onChange={(e) => update('workType', e.target.value)} /></label><label className="wide">Description<textarea value={form.description} onChange={(e) => update('description', e.target.value)} rows="4" /></label><label className="wide">Available sizes<input value={form.sizes} onChange={(e) => update('sizes', e.target.value)} /></label><label className="wide">Image URL<input value={form.image} onChange={(e) => update('image', e.target.value)} required placeholder="https://..." /></label></div>{form.image && <img className="upload-preview" src={form.image} alt="Preview" />}<button className="button primary" type="submit"><Upload size={16} /> Add design</button></form>}</main>; }
function Footer({ go }) { return <footer><div><button className="wordmark" onClick={() => go('home')}>Vasavi<span>Boutiques</span></button><p>Blouses with a point of view.</p></div><div className="footer-meta"><span>Hyderabad, India</span><span>© 2026 Vasavi</span></div></footer>; }

export default App;
